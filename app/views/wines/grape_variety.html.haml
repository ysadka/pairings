%h3 Select your wine

%ul
  -@varieties.each do |grape|
    -grape.wines.each do |wine|
      %li.row
        .columns.large-6
          = link_to " #{wine.vintage} #{wine.grape.name} #{wine.winery.appellation}", wine_path(wine.id)
